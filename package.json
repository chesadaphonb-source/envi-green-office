{
  "name": "green-office-envi",
  "version": "1.0.0",
  "private": true,
  "description": "Green Office ENVI static site and private Drive catalog pipeline",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check:legacy": "node scripts/check.cjs",
    "catalog:fetch": "node scripts/fetch-catalog.cjs",
    "build": "node scripts/build-site.cjs",
    "preview:build": "node scripts/build-site.cjs --preview",
    "preview": "node scripts/serve-site.cjs"
  }
}
